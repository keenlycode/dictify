# Default Role Session

Runtime is not initialized yet.

This file reserves the default role runtime contract. Configure a concrete
stateful session here only after Um confirms the runtime, CLI, model/provider,
tool access, transport, and output mode.

```yaml
cli: unconfigured
role: default
model: unconfigured
reasoning: unconfigured
session: stateful
cwd: /home/nitipit/space/code/umlab/dictify
tools: []
output:
  final_only: unsupported
  mode: cli-default
transport:
  kind: unconfigured
  manager: unconfigured
  session_name: unconfigured
  close: explicit-only
  capture: bounded-tail
```

## Commands

### Start

```bash
# Not configured.
```

### Run Task

```bash
# Not configured.
```

### Capture Result

```bash
# Not configured.
```

### Interrupt

```bash
# Not configured.
```

### Close

```bash
# Not configured.
```
