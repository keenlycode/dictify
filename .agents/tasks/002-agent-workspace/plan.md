# Plan

1. Migrate existing task state from root `tasks/` into `.agents/tasks/`.
2. Add `AGENTS.md` with the repository workspace convention.
3. Add a compact `.agents/README.md` workspace map.
4. Update default-agent and Dictify skill instructions.
5. Validate TOML and review the diff.
