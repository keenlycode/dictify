# Lean Packaged Usage Skill

## Goal

Make the packaged `dictify-usage` skill concise and portable for installed package environments.

## Scope

- Simplify `src/dictify/ai_skills/dictify-usage/SKILL.md`.
- Avoid repository checkout assumptions such as `src/`, `tests/`, or `dev/cli`.
- Keep guidance centered on bundled `references/` and installed public API.
