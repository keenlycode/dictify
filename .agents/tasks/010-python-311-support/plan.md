# Plan

1. Inspect Python version metadata and internal typing syntax.
2. Patch metadata and source syntax for Python 3.11 compatibility.
3. Update user-facing Python version docs.
4. Refresh `uv.lock` metadata.
5. Validate with tests, Ruff, type check, and package/import smoke checks as practical.
6. Do a final review pass for missed Python 3.12-only references.
